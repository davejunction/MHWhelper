<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN" "http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">  
<hibernate-mapping package="cn.edu.zucc.personplan.model">  
    <!-- 标识一个类的xml映射,name为类名,table为表名 -->  
    <class name="BeanPlan" table="tbl_plan">
        <!-- 主键的设置,generator标识主键生成方式,因为native便于数据库的移植所以.嘿嘿!你懂的 -->  
        <id name="plan_id" type="int">  
            <column name="plan_id"/>  
            <generator class="assigned"/>  
        </id>  
        <!-- 这里配了该类一个属性街道名称 name属性字段,column,数据库对应列 -->  
        <property name="user_id" type="string" column="user_id" />  
        <!-- 这里同上,写法不同而已 -->  
        <property name="plan_order" type="int" column="plan_order" />
        <property name="plan_name" type="string" column="plan_name" />
        <property name="create_time" type="timestamp" column="create_time" />
        <property name="step_count" type="int" column="step_count" />
        <property name="stcount" type="int" column="start_step_count" />
		<property name="ftcount" type="int" column="finish_step_count" />
      	
    </class>
    <class name="BeanStep" table="tbl_step">
        <!-- 主键的设置,generator标识主键生成方式,因为native便于数据库的移植所以.嘿嘿!你懂的 -->  
        <id name="step_id" type="int">  
            <column name="step_id"/>  
            <generator class="assigned"/>  
        </id>  
        <!-- 这里配了该类一个属性街道名称 name属性字段,column,数据库对应列 -->  
        <property name="plan_id" type="int" column="plan_id" />  
        <!-- 这里同上,写法不同而已 -->  
        <property name="step_order" type="int" column="step_order" />
        <property name="step_name" type="string" column="step_name" />
        <property name="pbtime" type="timestamp" column="plan_begin_time" />
        <property name="petime" type="timestamp" column="plan_end_time" />
        <property name="rbtime" type="timestamp" column="real_begin_time" />
		<property name="retime" type="timestamp" column="real_end_time" />
      	
    </class>
	<class name="BeanUser" table="tbl_user">
        <!-- 主键的设置,generator标识主键生成方式,因为native便于数据库的移植所以.嘿嘿!你懂的 -->  
        <id name="user_id" type="string">  
            <column name="step_id"/>  
            <generator class="assigned"/>  
        </id>  
        <!-- 这里配了该类一个属性街道名称 name属性字段,column,数据库对应列 -->  
        <property name="user_pwd" type="string" column="plan_id" />  
        <!-- 这里同上,写法不同而已 -->  
        <property name="reg_time" type="timestamp" column="reg_time" />
      	
    </class>

</hibernate-mapping>  
